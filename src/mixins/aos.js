import AOS from 'aos';
import 'aos/dist/aos.css';

export default {
  setup() {

    onMounted(() => {
      // AOS.init({
      //   // Add your AOS initialization options here
      // });

      AOS.init({});
    });

    
  },
};
